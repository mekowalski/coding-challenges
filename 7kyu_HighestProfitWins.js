function minMax(arr){
  return [Math.min(...arr), Math.max(...arr)]
}

// L2: return the Math minimum and Math maximum of the array, use spread operator for including both
// L2: integers in a combined array
